/**
 * A component that controls the audio element passed in.
 * Appears alongside the "Create Transcript" button to allow the user
 * to review the audio before submitting for transcription.
 *
 * @param audio {HTMLAudioElement} - The audio element to control
 */
export default function AudioControls({ audio }: { audio: HTMLAudioElement }) {

}